# 写真の置き場所

撮影した写真をこのフォルダーへ置きます。

ファイル名と表示先の対応は `assets/js/my-collection.js` で指定します。
たとえば公式ID 474の写真を表示する場合は、次のように記入します。

- 写真をこのフォルダーへ置く
- `my-collection.js` のID 474に `photo: "assets/photos/実際のファイル名.jpg"` を記入する

縦横どちらの写真でも表示できます。推奨は横位置・1600px以上です。
